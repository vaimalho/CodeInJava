package multithreading;

public class ProducerConsumer {

    // Consumer T is responsible to consume items from the Queue

    class ConsumerThread{

 /*       consume(){

            synchronized(q){
                if( q is empty)
                    q.wait();
                else
                    consumeItems;
            }

        }
*/
    }

    // Producer T is responsible to Produce items to the Queue

    class ProducerThread{
/*
        produce(){
            Produce items and add to Queue
                    q.notify();
        }
 */
    }
}
